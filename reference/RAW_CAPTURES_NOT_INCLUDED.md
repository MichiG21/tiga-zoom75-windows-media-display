# Raw captures intentionally not included

The clean shareable bundle intentionally omits large `.pcapng` / `.pklg` captures and local runtime logs. They are useful historical evidence but can contain machine/device/session metadata and make the archive much larger. The protocol facts derived from them are summarized in `../docs/PROTOCOL_NOTES.md`.
