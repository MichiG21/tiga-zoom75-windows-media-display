diff --git a/boards/zoom75-tiga/examples/known_good_red_green_animation_upload.rs b/boards/zoom75-tiga/examples/known_good_red_green_animation_upload.rs
index d5fa18f..1c446d0 100644
--- a/boards/zoom75-tiga/examples/known_good_red_green_animation_upload.rs
+++ b/boards/zoom75-tiga/examples/known_good_red_green_animation_upload.rs
@@ -37,8 +37,11 @@ const FRAME_BYTES: usize = WIDTH * HEIGHT * 2;
 const FRAME_COUNT: usize = 2;
 const CHUNK_SIZE: usize = 16;
 
-const MEDIA_ACK_TIMEOUT: Duration = Duration::from_secs(3);
+// Calibrated from a clean USBPcap capture: normal FC/FE00 ACKs arrive in
+// ~45-56 ms, with observed slow-but-valid ACKs up to ~118 ms after startup.
+const MEDIA_ACK_TIMEOUT: Duration = Duration::from_millis(250);
 const MAX_RETRIES: usize = 5;
+const RETRY_BACKOFF: Duration = Duration::from_millis(25);
 const INTER_PACKET_DELAY: Duration = Duration::from_millis(5);
 const SECTOR_PAUSE: Duration = Duration::from_millis(100);
 
@@ -154,7 +157,7 @@ fn send_page_with_retry(
                 println!(
                     "{label}: no media ACK on attempt {attempt}/{MAX_RETRIES} ({e}); retrying..."
                 );
-                sleep(Duration::from_millis(150));
+                sleep(RETRY_BACKOFF);
             }
             Err(e) => {
                 return Err(format!(
@@ -190,6 +193,8 @@ fn main() -> Result<(), Box<dyn std::error::Error>> {
     println!("Captured media SHA-256: {CAPTURED_MEDIA_SHA256}");
     println!("ACK rule        : FC / FE00 media ACK only");
     println!("Retries         : max {MAX_RETRIES} per page");
+    println!("ACK timeout     : 250 ms (USBPcap-calibrated)");
+    println!("Retry backoff   : 25 ms");
     println!("Pacing          : 5 ms after ACK + 100 ms every 256 pages");
     println!();
 
